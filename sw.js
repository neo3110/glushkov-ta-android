# Глушков·ТА — сборка APK и публикация в RuStore

Пошагово: от исходников к приложению в магазине. Порядок важен — ключ подписи
делается один раз и до первой сборки, потому что потерять его после публикации
означает потерять возможность обновлять приложение.

- [1. Как это устроено](#1-как-это-устроено)
- [2. Ключ подписи](#2-ключ-подписи-один-раз-и-навсегда)
- [3. Сборка APK](#3-сборка-apk)
- [4. Проверка на телефоне](#4-проверка-на-телефоне-до-публикации)
- [5. Аккаунт в RuStore Консоль](#5-аккаунт-в-rustore-консоль)
- [6. Загрузка и карточка приложения](#6-загрузка-и-карточка-приложения)
- [7. Модерация](#7-модерация-и-публикация)
- [8. Обновления](#8-обновления)
- [9. Если что-то пошло не так](#9-если-что-то-пошло-не-так)

---

## 1. Как это устроено

Приложение — это то же самое веб-приложение, что было в PWA, упакованное в
Android-оболочку. Никакого переписывания на Kotlin: весь план тренировок,
расчёт КПШ, зоны интенсивности и справочник остались тем же `index.html`.
Он лежит внутри APK в папке `assets/www` и открывается в системном WebView.

Три вещи, которые отличают эту сборку от простого «открыть сайт в WebView»:

**Адрес.** Страница отдаётся не через `file://`, а через `WebViewAssetLoader`
по адресу `https://appassets.androidplatform.net/`. Это виртуальный адрес, в
сеть он не ходит. Нужен он ради `localStorage`: на `file://` браузерный движок
считает страницу «origin-less» и режет хранилище, а тут данные атлета живут
ровно как в браузере и переживают обновление приложения.

**Шрифты.** Unbounded и Manrope раньше тянулись с Google Fonts. Теперь их
копии (кириллица + латиница, начертания 400–800) лежат в
`assets/www/fonts/files`. Поэтому приложению вообще не нужен интернет — в
`AndroidManifest.xml` нет даже разрешения `INTERNET`. Для модерации это плюс:
разрешений ноль, объяснять нечего.

**Service worker отключён.** В PWA он давал офлайн-режим. В APK файлы и так
локальные, а вот вреда от него было бы много: его кэш пережил бы установку
новой версии, и пользователь после обновления увидел бы старое приложение.
Скрипт `prepare_assets.py` при упаковке заменяет регистрацию SW на снятие
регистрации — на случай, если кто-то раньше ставил PWA на тот же телефон.

Экспорт резервной копии тоже пришлось подпереть: в WebView ссылка
`<a download>` с blob-адресом молча ничего не делает. При загрузке страницы
в неё подставляется маленький шим, который дочитывает blob и передаёт файл в
Kotlin, а тот кладёт его в «Загрузки» (Android 10+) или открывает
«Поделиться». Веб-код при этом не тронут — в браузере всё работает по-старому.

---

## 2. Ключ подписи (один раз и навсегда)

RuStore не принимает неподписанные файлы, и подпись всех будущих версий должна
совпадать с подписью первой. Потерял ключ — обновить опубликованное приложение
уже нельзя, только выкладывать новое с другим package name и терять
установки. Поэтому: сделать, положить в надёжное место, сделать копию.

Нужна Java (в Android Studio она уже есть, отдельно — любой JDK 17).

```bash
keytool -genkeypair -v \
  -keystore glushkov-ta.jks \
  -alias glushkov \
  -keyalg RSA -keysize 4096 \
  -validity 10000 \
  -storetype PKCS12
```

`keytool` спросит пароль хранилища, потом ФИО/организацию/город — заполняй
как есть, на витрину это не попадает. `-validity 10000` — это ~27 лет, меньше
ставить не надо.

На выходе `glushkov-ta.jks`. Запиши в менеджер паролей три вещи: пароль
хранилища, пароль ключа, алиас (`glushkov`). Сам файл — в облако и на флешку.
**В git его класть нельзя**, `.gitignore` это уже запрещает.

Дальше скопируй `keystore.properties.example` в `keystore.properties` (в корне
проекта) и впиши свои значения:

```properties
storeFile=/home/user/keys/glushkov-ta.jks
storePassword=...
keyAlias=glushkov
keyPassword=...
```

---

## 3. Сборка APK

Три пути. Первый не требует ставить ничего вообще — рекомендую его, если
Android Studio на компьютере ещё нет (это ~10 ГБ).

### Вариант А. GitHub Actions — собирает в облаке

1. Создай на GitHub **приватный** репозиторий и залей туда всю папку проекта.
2. Ключ надо передать в сборку через секреты. Сначала преврати `.jks` в текст:

   ```bash
   base64 -w0 glushkov-ta.jks > keystore.b64     # Linux
   base64 -i glushkov-ta.jks | tr -d '\n' > keystore.b64   # macOS
   ```

   В Windows PowerShell:

   ```powershell
   [Convert]::ToBase64String([IO.File]::ReadAllBytes("glushkov-ta.jks")) > keystore.b64
   ```

3. В репозитории: **Settings → Secrets and variables → Actions → New
   repository secret**. Добавь четыре секрета:

   | Имя | Значение |
   |---|---|
   | `KEYSTORE_BASE64` | содержимое `keystore.b64` одной строкой |
   | `KEYSTORE_PASSWORD` | пароль хранилища |
   | `KEY_ALIAS` | `glushkov` |
   | `KEY_PASSWORD` | пароль ключа |

4. Вкладка **Actions → «Сборка APK» → Run workflow**.
5. Через 5–10 минут внизу страницы запуска появится артефакт
   `glushkov-ta-apk` — это zip, внутри `app-release.apk`.

Если секреты не заданы, сборка всё равно пройдёт, но соберётся **отладочная**
версия. Так сделано специально: неподписанный release-APK Android установить
не даст вообще, а debug подписан служебным ключом и на телефон встанет. Это
удобно для первой проверки — можно убедиться, что проект собирается и работает,
ещё до возни с ключом. Для RuStore нужна именно release-сборка с твоим ключом.

Одна оговорка про debug: в нём не работает минификация R8, поэтому кнопку
«Экспорт данных» обязательно проверь ещё раз уже на release-сборке.

### Вариант Б. Android Studio

1. Поставь Android Studio (свежую стабильную) и открой папку проекта:
   **File → Open** → выбрать корень с `settings.gradle`.
2. При первом открытии Studio сама скачает Android SDK 35 и создаст Gradle
   wrapper (файл `gradle/wrapper/gradle-wrapper.jar` намеренно не входит в
   поставку — это бинарник). Если она спросит про версию Gradle, укажи 8.9.
3. Дождись, пока внизу пройдёт **Gradle Sync**.
4. **Build → Generate Signed App Bundle / APK → APK → Next**.
5. Укажи `glushkov-ta.jks`, пароли и алиас, выбери вариант **release**,
   жми **Create**.
6. APK окажется в `app/build/outputs/apk/release/`.

Через Android Studio удобно и просто запускать на телефоне: включи на нём
режим разработчика и отладку по USB, подключи кабелем и нажми ▶.

### Вариант В. Командная строка

Нужны JDK 17, Android SDK (переменная `ANDROID_HOME`) и Gradle 8.9.

```bash
cd glushkov-ta-android
gradle wrapper --gradle-version 8.9   # один раз, создаст ./gradlew
./gradlew assembleRelease
```

Готовый файл: `app/build/outputs/apk/release/app-release.apk`.

Полезные команды:

```bash
./gradlew assembleDebug    # отладочная сборка, ставится рядом с релизной
./gradlew clean            # если сборка ведёт себя странно
./gradlew bundleRelease    # AAB вместо APK, см. п.6
```

Проверить, что APK действительно подписан:

```bash
$ANDROID_HOME/build-tools/35.0.0/apksigner verify --print-certs app-release.apk
```

---

## 4. Проверка на телефоне до публикации

Перекинь APK на телефон (мессенджером, кабелем, облаком), открой и разреши
установку из этого источника. Модератор будет смотреть живое приложение, так
что пройди этот список сам:

- [ ] Иконка на рабочем столе — штанга, не серый робот
- [ ] Онбординг проходится, профиль сохраняется
- [ ] **Закрыть приложение целиком и открыть заново — данные на месте.**
      Это главная проверка: если тут пусто, значит сломался localStorage
- [ ] Нижняя навигация из 6 вкладок не залезает под жестовую полосу
- [ ] Шапка не уезжает под вырез камеры
- [ ] Клавиатура не закрывает поля ввода веса (проверь на старом телефоне,
      если есть — на Android 10 и ниже это самое хрупкое место)
- [ ] Кириллица набрана Unbounded и Manrope, а не системным шрифтом
      (если шрифты не подхватились — заголовки будут выглядеть как обычный текст)
- [ ] Журнал → «Экспорт данных» → файл появился в «Загрузках»
- [ ] Журнал → «Импорт из файла» → выбор файла открывается, данные встают
- [ ] Кнопка «назад» из главного экрана предлагает выйти, а не закрывает сразу
- [ ] Графики ПМ рисуются, анимации техники в «Гиде» проигрываются
- [ ] Включи в системе авиарежим и пройди все вкладки — всё должно работать

Заодно сделай **скриншоты для витрины**: 4–8 штук, кнопка питания + громкость
вниз. Снимай экраны, которые продают приложение: План с готовой тренировкой,
график ПМ с маркером старта, зональный анализ, вкладка Тело, Гид с анимацией
техники.

---

## 5. Аккаунт в RuStore Консоль

1. Открой **console.rustore.ru** и войди через VK ID.
2. Выбери тип аккаунта:
   - **Физическое лицо** — регистрация проще всего, достаточно VK ID и
     подтверждения данных в форме. Для бесплатного приложения этого хватает.
   - **ИП / юрлицо** — нужна усиленная квалифицированная электронная подпись
     (УКЭП) и плагин КриптоПро. Это путь для тех, кто планирует брать деньги:
     с 1 февраля 2026 платёжные инструменты RuStore (платные приложения,
     подписки, встроенные покупки) доступны только ИП и юрлицам.

   Наше приложение бесплатное и ничего не продаёт, так что физлица достаточно.
   Размещение в RuStore бесплатное.
3. Дождись подтверждения аккаунта.

---

## 6. Загрузка и карточка приложения

В консоли: **Приложения → Добавить приложение**.

**Файл сборки.** RuStore принимает и APK, и AAB. Бери **APK** — с ним проще:
AAB требует отдельно загрузить в консоль закрытый ключ подписи через утилиту
PEPK, потому что RuStore, в отличие от Google Play, сам файлы не переподписывает.
Для одного человека с одним приложением эта возня лишняя.

Требования к сборке, которым проект уже соответствует:

| Требование | У нас |
|---|---|
| targetSdkVersion не ниже 28 | 35 |
| подписан действующим ключом | да, п.2 |
| 64-битные библиотеки, если есть нативный код | нативного кода нет |
| package name уникален и неизменен после публикации | `ru.glushkov.ta` |

**Package name менять после публикации нельзя.** Если `ru.glushkov.ta` не
подходит — поменять надо *сейчас*, до первой загрузки: `namespace` и
`applicationId` в `app/build.gradle`, папку `app/src/main/java/ru/glushkov/ta`
и строку `package` в первой строке `MainActivity.kt`.

**Тексты карточки** — готовые лежат в `store/карточка-приложения.md`, оттуда
можно копировать. Ограничения: краткое описание до 80 символов, подробное —
до 4000.

**Категория.** «Здоровье и фитнес» или «Спорт», в зависимости от того, что
предложит консоль.

**Возрастной рейтинг** проставляется автоматически по анкете о содержимом.
Отвечай честно; насилия, азартных игр и пользовательского контента здесь нет.

**Иконка.** PNG 512×512 без прозрачного фона, до 3 МБ, без текста и рамок.
Готовые лежат в `store/`: `icon-512-rustore.png` (чистая штанга) и
`icon-512-rustore-original.png` (та же штанга с российским флагом, как в
исходном PWA). Бери любую.

**Скриншоты** — те, что снял в п.4. Минимум 4, вертикальные, 1080×1920.
Первые два самые важные, их видят в каталоге.

**Политика конфиденциальности.** Консоль требует ссылку. Готовый текст —
в `store/политика-конфиденциальности.md`. Опубликовать его можно бесплатно:
создай публичный репозиторий на GitHub, положи туда файл, включи
**Settings → Pages**, и получишь адрес вида
`https://<логин>.github.io/<репозиторий>/`. Подойдёт и любой другой хостинг,
и даже страница ВКонтакте — главное, чтобы ссылка открывалась без входа.

**Email поддержки** — рабочая почта, на неё будут писать пользователи и
модерация.

---

## 7. Модерация и публикация

Отправь на модерацию. Тип публикации выбери **вручную** — тогда после
одобрения приложение появится в магазине только когда ты нажмёшь «Опубликовать»,
и будет время всё перепроверить.

Что чаще всего приводит к отказу и чего у нас нет по построению:

- **Запрещённые разрешения.** У приложения их ноль — самый частый повод для
  отказа отпадает сам.
- **Неработающая ссылка на политику конфиденциальности.** Открой её в режиме
  инкогнито перед отправкой.
- **Скриншоты не от этого приложения** или рамки телефонов с логотипами Apple,
  Google, Samsung. Снимай реальные экраны без рамок.
- **Обещания медицинского эффекта.** Приложение про спорт, а не про лечение;
  в описании и внутри есть дисклеймер, что это методический каркас и не замена
  тренеру и врачу. Не убирай его.
- **Чужое имя в названии.** В названии стоит фамилия реального тренера. Если
  это не ты — нужно его согласие, модерация вправе спросить. Поменять название
  можно в `app/src/main/res/values/strings.xml` (подпись под иконкой) и в теге
  `<title>` внутри `assets/www/index.html` (шапка внутри приложения).

Если пришёл отказ — в консоли будет причина. Исправляешь, поднимаешь
`versionCode` и заливаешь заново.

---

## 8. Обновления

Каждая новая загрузка должна иметь **больший `versionCode`**, иначе консоль
файл не примет. В `app/build.gradle`:

```groovy
versionCode 2          // было 1 — просто +1, всегда
versionName "1.1"      // это видит пользователь, тут любая схема
```

Дальше: собрать тем же ключом → в консоли открыть приложение → **Версии →
создать новую версию** → загрузить APK → заполнить «Что нового» → на модерацию.
Обновления обычно проверяют быстрее первой публикации.

**Если менялось само веб-приложение** (`index.html`), не копируй его в
`assets/www` руками — прогони через скрипт, он снимет Google Fonts и
service worker:

```bash
python3 prepare_assets.py <папка_с_PWA> <папка_с_woff2> app/src/main/assets/www
```

Скрипт падает с ошибкой, если не нашёл, что патчить, — это защита от молчаливой
сборки со сломанными шрифтами.

---

## 9. Если что-то пошло не так

**Белый экран вместо приложения.** Файлы не попали в `assets/www` или сломан
путь. Подключи телефон к компьютеру, открой в Chrome `chrome://inspect` —
отладочная сборка (`assembleDebug`) видна там как обычная страница со всей
консолью.

**Данные пропадают после перезапуска.** Проверь, что в `configureWebView()`
остался `domStorageEnabled = true` и что адрес загрузки — `https://appassets…`,
а не `file://`.

**Заголовки набраны системным шрифтом.** Не доехали woff2. Проверь, что
`app/src/main/assets/www/fonts/files/` не пустая (там должно быть 20 файлов).

**Экспорт молчит.** В release-сборке R8 мог переименовать методы моста.
Убедись, что `app/proguard-rules.pro` на месте и в нём есть правило про
`@android.webkit.JavascriptInterface`.

**Gradle sync падает на скачивании зависимостей.** Репозитории `google()` и
`mavenCentral()` из России могут не открываться напрямую — понадобится VPN на
время сборки. На GitHub Actions такой проблемы нет, сборка идёт с их серверов.

**Нижняя панель под системной навигацией.** Скорее всего сломан обработчик
инсетов в `MainActivity`. Он один, ищи `setOnApplyWindowInsetsListener`.

---

## Что ещё стоит знать

Требования магазинов меняются. Перед публикацией пробегись глазами по
актуальной документации: **rustore.ru/help** — раздел для разработчиков.
Консоль при загрузке сама показывает лимиты по размерам картинок и текстов,
это надёжнее любой инструкции.

Данные атлета живут только на его телефоне. Автоматический бэкап Android
(`allowBackup="true"`) заберёт их в облако, если он включён на устройстве, но
полагаться на это нельзя. Скажи пользователям про кнопку «Экспорт данных» в
Журнале — это единственная гарантия, что четыре месяца тренировочного журнала
не исчезнут вместе с телефоном.
